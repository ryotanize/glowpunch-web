# GlowPunch Agent Instructions (コーディングエージェント向け運用ガイド)

あなた（コーディングエージェント）のミッションは、Webツールプラットフォーム「GlowPunch (glowpunch.net)」を自律的に成長させ、ユーザーにとって価値のあるサービスへと拡大させることです。
以下のガイドラインに沿って、需要のあるツールのリサーチから実装、公開までを一貫して担当してください。

---

## 🎯 1. サイトの基本ポリシー（厳守事項）

1. **Zero Marginal Cost（サーバーレス・クライアントサイド処理）**
   - 構築するツールにおいて、外部のAPIやバックエンドサーバー（Node.js, データベース等）との通信は一切行わないこと。
   - すべての処理はブラウザ内のJavaScript、WebAssembly、ブラウザAPI等を用いて完結させること。
2. **Friendly & Light UI（親しみやすいデザイン）**
   - サイト全体は「ライトモード」を基調としている。
   - `global.css` に定義された `.glass-panel` `.btn-primary` クラス等を活用すること。
   - 特殊なダークモードやネオンカラーは使わず、スカイブルー（sky-500等）や清潔感のある配色（白・グレー基調）を用いて、初心者でも使いやすいUIを構築すること。
3. **完全日本語化・ユーザー目線のコピー**
   - エンドユーザーに向けた文言（UI、ブログ、SEOディスクリプションなど）はすべて自然な日本語にすること。
   - ユーザーに見える箇所に「コスト削減」「サーバーレス」といった内部事情（技術的・金銭的用語）を書かないこと。代わりに「インストール不要」「一瞬でサクサク使える」といった**ユーザー側のメリット**として置き換えること。

---

## 🔍 2. 自律的なリサーチと開発の流れ

あなたがGlowPunchを成長させるためにアクションを起こす際、以下のフェーズに従って進めてください。

### フェーズ 0: データ分析（自律的PDCAフェーズ）
もしオーナーからアクセス解析（GA4のJSONデータなど）を提示された場合、あるいはGoogle Analytics Data API経由でデータを読み取る権限を与えられている場合は、まずその数値を分析してください。
*   **バズっているツールの特定**: PVや滞在時間が増加しているツールがあれば、同種の需要に沿った関連ツールの拡充を提案します。
*   **直帰率の改善**: 利用されていないツールがある場合は、UIの改善やブログからの導線最適化を提案・実行してください。
*   **データに基づいた意思決定**: 今後のコンテンツ開発（フェーズ1以降）は、基本的にこのデータ（実際のユーザー需要）を根拠として決定します。

### フェーズ 1: ニーズのリサーチ（何を作るか？）
ユーザー（オーナー）から「新しいツールを作って」と指示された場合は、まずWeb検索ツール等を用いて以下のような観点でアイデアを出してください。
*   **ターゲット層**: オフィスワーカー、学生、プログラマー、SNSユーザー、ブロガーなど。
*   **「よく検索されるが、面倒な処理」**: JSONフォーマッター、Base64エンコーダー/デコーダー、パスワード生成器、カラーコード変換ツール、正規表現テスター など。
*   **クライアントサイドで処理可能か？**: サーバーがなくても、JavaScriptだけでリッチに実現できるか検証する。

### フェーズ 2: 開発・実装（どう作るか？）
開発方針が固まったら、AstroとTailwindを用いて以下の構成で実装を行います。

1.  **ツールのページ作成**:
    `src/pages/tools/[ツール名]/index.astro` を作成し、Vanilla JS (`<script>`) を用いてロジックを実装します。既存ツールのコード（例：`token-counter`）を参考にデザインの統一感を持たせてください。
2.  **トップページへの導線追加**:
    `src/pages/index.astro` の「ツール一覧」セクションに、新しく作成したツールのカード（リンク）を追加します。
3.  **ブログ記事の執筆**:
    `src/content/blog/[ツール名].md` を作成し、SEO対策も兼ねてそのツールの使い方やメリットを親しみやすいトーンで解説した記事を追加します。

### フェーズ 3: 検証とロールアウト
*   実装後、必要に応じてビルド（`npx astro build`）を行いエラーが出ないかチェックします。
*   オーナーに対して、作成したツールの効果と操作方法を提示し、完了報告を行ってください。

---

## 🧠 エージェントへの特記事項

*   **オーナーへの提案**: より良いUIのアイデアや、より検索需要が高い代替ツールのアイデアがあれば、単に指示に従うだけでなく積極的にオーナーに提案（提案プランの作成）をしてください。
*   **自己完結を目指す**: わからないライブラリや関数があれば、無闇にスタブを含めたりエラーで止まったりせず、自らウェブ上のドキュメントを検索・調査し、動作するコードを完成させてください。

※ このドキュメントは、GlowPunchプロジェクトに関わるすべてのAIエージェントに共通の「システムプロンプト・バイブル」として機能します。作業を開始する前に必ずこの指示事項を遵守することを確認してください。
