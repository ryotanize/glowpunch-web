---
import BaseLayout from '../../../layouts/BaseLayout.astro';
---

<BaseLayout title="LLMトークンカウンター - GlowPunch" description="LLM向けの入力本文をブラウザ内でトークン計算します。処理用の外部APIへ入力本文を送信しません。">
  <div class="flex flex-col gap-6">
    <div class="mb-4">
      <h1 class="text-3xl md:text-4xl font-bold text-slate-800 tracking-tight mb-2">LLMトークン<span class="text-purple-500">カウンター</span></h1>
      <p class="text-slate-600 text-lg">ChatGPT等のプロンプトトークン数を瞬時に計算。処理対象テキストは外部サーバーへ送信しません。</p>
    </div>

    <!-- Stats Dashboard -->
    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-2">
      <div class="glass-panel p-5 flex flex-col items-center justify-center relative overflow-hidden group">
        <span class="text-slate-500 text-[0.8rem] font-medium mb-1 tracking-wider">トークン数 (o200k_base)</span>
        <span class="text-4xl md:text-5xl font-black text-purple-600" id="stat-tokens">0</span>
      </div>
      <div class="glass-panel p-5 flex flex-col items-center justify-center relative overflow-hidden group">
        <span class="text-slate-500 text-sm font-medium mb-1 tracking-wider">文字数</span>
        <span class="text-4xl md:text-5xl font-black text-slate-800" id="stat-chars">0</span>
      </div>
      <div class="glass-panel p-5 flex flex-col items-center justify-center relative overflow-hidden group">
        <span class="text-slate-500 text-[0.8rem] font-medium mb-1 tracking-wider">1トークンあたりの文字</span>
        <span class="text-4xl md:text-5xl font-black text-slate-800" id="stat-ratio">0.0</span>
      </div>
      <div class="glass-panel p-5 flex flex-col items-center justify-center relative overflow-hidden group">
        <span class="text-slate-500 text-[0.8rem] font-medium mb-1 tracking-wider">計測対象</span>
        <span class="text-lg font-bold text-slate-800" id="stat-cost">入力本文のみ</span>
      </div>
    </div>

    <!-- Text Area -->
    <div class="glass-panel p-1 rounded-2xl relative group focus-within:ring-2 focus-within:ring-purple-500/50 transition-all bg-white border border-slate-300 shadow-inner">
      <textarea 
        id="text-input" aria-label="計測するテキスト"
        class="w-full h-80 bg-transparent rounded-xl outline-none border-none p-6 text-slate-800 placeholder-slate-400 resize-y focus:outline-none focus:ring-0 font-sans text-lg appearance-none"
        placeholder="プロンプトを入力、または貼り付けてください..."
      ></textarea>
    </div>

    <!-- Actions -->
    <div class="flex flex-wrap gap-4 mt-2">
      <button id="btn-copy" class="px-6 py-3 rounded-xl font-bold text-white bg-purple-500 border border-transparent transition-all duration-300 hover:bg-purple-600 hover:shadow-md active:scale-95 flex items-center gap-2">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
        テキストをコピー
      </button>
      <button id="btn-clear" class="px-6 py-3 rounded-xl font-bold text-slate-600 bg-slate-100 hover:bg-slate-200 border border-slate-300 transition-colors flex items-center gap-2 active:scale-95">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18"/><path d="M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6"/><path d="M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2"/></svg>
        クリア
      </button>
    </div>

    <div class="mt-12">
    </div>

    <!-- Tool guidance -->
    <article class="prose prose-slate mt-16 max-w-none border-t border-slate-200 pt-10">
      <h2>LLMトークンカウンターについて</h2>
      <p>このツールは、入力された本文の「トークン数」をo200k_base方式でブラウザ内で計算するツールです。</p>
      <h3>処理対象テキストの扱い</h3>
      <ul>
        <li><strong>ブラウザ内処理:</strong> トークン計算はブラウザ上で行い、入力本文を処理用の外部APIへ送信しません。業務データは所属組織のルール、端末環境、保存先も確認してください。</li>
        <li><strong>最新のエンコーディング:</strong> 内部では <code>o200k_base</code> エンコーディングをブラウザに移植したライブラリを使用しており、本文を計測します。モデルやメッセージ構造によって実際の使用量は異なります。</li>
      </ul>
      <p>API費用の見積もりや、コンテキストウィンドウの制限確認にご活用ください。</p>
    </article>
  
    <div class="mt-8 border-t border-slate-200 pt-8">
      <details class="group bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden">
        <summary class="flex items-center justify-between p-5 font-bold text-slate-800 cursor-pointer bg-slate-50 hover:bg-slate-100 transition-colors">
          <span>LLMトークンカウンターについて（使い方・FAQ）</span>
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="transition-transform duration-300 group-open:-rotate-180 text-slate-500"><polyline points="6 9 12 15 18 9"></polyline></svg>
        </summary>
        <div class="p-6 text-slate-600 space-y-6">
          <div class="prose prose-slate max-w-none">
            <h3>LLMトークンカウンターについて</h3>
            <p>LLMを利用する際のAPIコスト見積もりやコンテキストウィンドウ管理に役立つ、ブラウザ内のトークン計算ツールです。</p>

            <h3>よくある質問 (FAQ)</h3>

            <h4>Q. トークンとは何ですか？</h4>
            <p>A. トークンとは、LLM（大規模言語モデル）がテキストを処理する際の最小単位です。英語では1単語が1〜2トークン、日本語では1文字が1〜3トークン程度になります。APIの料金はトークン数に基づいて計算されるため、事前にトークン数を把握することがコスト管理に重要です。</p>

            <h4>Q. 入力したプロンプトはサーバーに送信されますか？</h4>
            <p>A. トークン計算はブラウザ内で行い、入力本文を処理用の外部APIへ送信しません。サイト配信やライブラリ取得に伴う通信は別途発生します。</p>

            <h4>Q. どのモデルのトークナイザーに対応していますか？</h4>
            <p>A. o200k_base方式の本文計測です。異なる方式のモデルや、会話構造・画像を含む入力の使用量とは一致しません。</p>
          </div>
        </div>
      </details>
    </div>

  </div>

  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "SoftwareApplication",
          "name": "LLMトークンカウンター",
          "operatingSystem": "All",
          "applicationCategory": "UtilitiesApplication",
          "offers": {
            "@type": "Offer",
            "price": "0",
            "priceCurrency": "JPY"
          },
          "description": "LLM向けの入力本文をブラウザ内でトークン計算する無料ツールです。処理用の外部APIへ入力本文を送信しません。"
        },
        {
          "@type": "FAQPage",
          "mainEntity": [
            {
              "@type": "Question",
              "name": "トークンとは何ですか？",
              "acceptedAnswer": {
                "@type": "Answer",
                "text": "トークンとは、LLM（大規模言語モデル）がテキストを処理する際の最小単位です。英語では1単語が1〜2トークン、日本語では1文字が1〜3トークン程度になります。APIの料金はトークン数に基づいて計算されるため、事前にトークン数を把握することがコスト管理に重要です。"
              }
            },
            {
              "@type": "Question",
              "name": "入力したプロンプトはサーバーに送信されますか？",
              "acceptedAnswer": {
                "@type": "Answer",
                "text": "トークン計算はブラウザ内で行い、入力本文を処理用の外部APIへ送信しません。サイト配信やライブラリ取得に伴う通信は別途発生します。"
              }
            },
            {
              "@type": "Question",
              "name": "どのモデルのトークナイザーに対応していますか？",
              "acceptedAnswer": {
                "@type": "Answer",
                "text": "o200k_base方式の本文計測です。異なる方式のモデルや、会話構造・画像を含む入力の使用量とは一致しません。"
              }
            }
          ]
        }
      ]
    }
  </script>
</BaseLayout>

<script>
  import { encode } from 'gpt-tokenizer/encoding/o200k_base';

  const input = document.getElementById('text-input') as HTMLTextAreaElement | null;
  const statTokens = document.getElementById('stat-tokens');
  const statChars = document.getElementById('stat-chars');
  const statRatio = document.getElementById('stat-ratio');
  const statCost = document.getElementById('stat-cost');
  const btnCopy = document.getElementById('btn-copy');
  const btnClear = document.getElementById('btn-clear');

  function updateStats() {
    if (!input || !statTokens || !statChars || !statRatio || !statCost) return;
    
    const text = input.value;
    
    // Characters
    const charCount = text.length;
    statChars.textContent = charCount.toLocaleString();
    
    // Tokens
    let tokenCount = 0;
    if (text.length > 0) {
      try {
        const tokens = encode(text);
        tokenCount = tokens.length;
      } catch (e) {
        console.error("Tokenization error:", e);
      }
    }
    statTokens.textContent = tokenCount.toLocaleString();
    
    // Ratio (Chars per Token)
    let ratio = 0;
    if (tokenCount > 0) {
      ratio = charCount / tokenCount;
    }
    statRatio.textContent = ratio.toFixed(2);
    
    statCost.textContent = '入力本文のみ';
  }

  input?.addEventListener('input', updateStats);

  btnCopy?.addEventListener('click', async () => {
    if (input) {
      try {
        await navigator.clipboard.writeText(input.value);
        const originalText = btnCopy.innerHTML;
        btnCopy.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-emerald-300"><polyline points="20 6 9 17 4 12"/></svg> コピーしました！`;
        setTimeout(() => {
          btnCopy.innerHTML = originalText;
        }, 2000);
      } catch (err) {
        console.error('Failed to copy text: ', err);
      }
    }
  });

  btnClear?.addEventListener('click', () => {
    if (input) {
      input.value = '';
      updateStats();
      input.focus();
    }
  });
  
  // initialize state
  updateStats();
</script>
