# Google Analytics 4 (GA4) 設定とエージェント連携ガイド

GlowPunchのアクセス解析を行い、将来的にコーディングエージェントがデータを読み取って自律的に改善を行えるようにするための設定手順書です。

---

## 1. Google Analyticsでのプロパティ作成と測定IDの取得
1. **Google Analyticsにアクセス**
   - [Google Analytics](https://analytics.google.com/) にアクセスし、Googleアカウントでログインします。
2. **アカウントとプロパティを作成**
   - 「管理（歯車アイコン）」設定から、「アカウントを作成」または「プロパティを作成」をクリックします。
   - プロパティ名: `GlowPunch` 、タイムゾーン: `日本` に設定します。
3. **データストリームの設定（測定IDの取得）**
   - プラットフォームとして **「ウェブ」** を選択します。
   - ウェブサイトのURLに `glowpunch.net` を入力し、ストリーム名も同様に入力して「ストリームを作成」をクリックします。
   - 完了画面で表示される **測定ID（`G-XXXXXXXXXX` のような文字列）** をコピーして控えておきます。

---

## 2. Cloudflare Pagesへの環境変数の設定
取得した測定IDをソースコードに直書きするのではなく、Cloudflare側から安全に注入します。

1. **Cloudflareダッシュボード**
   - `glowpunch` の Pages プロジェクト画面を開きます。
2. **環境変数の追加**
   - **[設定] > [環境変数]** の順にクリックします。
   - その下にある「変数を追加」をクリックし、以下のように入力します。
     - 変数名: `PUBLIC_GA_MEASUREMENT_ID`
     - 値: `G-XXXXXXXXXX` (先ほど取得した測定ID)
   - 「保存」をクリックします。
3. **再デプロイの実行**
   - 環境変数を反映させるため、**[デプロイ]** タブに戻り、最新のデプロイの右側にある「・・・」から **[デプロイの再試行]** をクリックして数分待ちます。
   - （またはローカルで適当なコミットをして `git push` すれば自動で反映されます）

---

## 3. 【今後の拡張】エージェントへの自動分析アクセス権付与
（※この手順はすぐに実行しなくても問題ありませんが、コーディングエージェントに分析を丸投げしたいタイミングで実施してください）

エージェントがプログラムからGAの生データを引っ張るためには、Google Cloud Platform上で「サービスアカウント」を発行し、GA側に「閲覧者権限」を持たせる必要があります。

1. **Google Cloud Platform (GCP)でAPIを有効化**
   - GCPコンソールにログインし、プロジェクトを作成します。
   - [APIとサービス] から **「Google Analytics Data API」** を検索して有効化します。
2. **サービスアカウントキー（JSON）の作成**
   - [IAMと管理] > [サービスアカウント] で新しいアカウントを作成します（例: `glowpunch-agent@...`）。
   - そのアカウントの「キー」タブから **新しいキーを作成(JSON)** してダウンロードします。（絶対にGitHubへコミットしないでください！）
3. **GA側でサービスアカウントに権限を付与**
   - Google Analyticsの管理画面に戻り、「プロパティのアクセス管理」を開きます。
   - 上記で作成したサービスアカウントのメールアドレスを「閲覧者（Viewer）」権限で追加します。
4. **エージェントへの紐付け（将来タスク）**
   - エージェント（Antigravity等）を実行する環境内で、ダウンロードしたJSONキーのパスを環境変数 `GOOGLE_APPLICATION_CREDENTIALS` として与えます。これでエージェントがデータを読み取れるようになります。

---
